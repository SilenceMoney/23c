\documentclass[withoutpreface]{cumcmthesis}

\usepackage{amsmath,amssymb,amsfonts,bm}
\usepackage{booktabs,tabularx,multirow,longtable}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{url}
\usepackage{hyperref}

\lstset{
    language=Matlab,
    basicstyle=\small\ttfamily,
    keywordstyle=\color{blue},
    commentstyle=\color{green!60!black},
    stringstyle=\color{red},
    breaklines=true,
    frame=single,
    numbers=left,
    numberstyle=\tiny,
    tabsize=4,
    showstringspaces=false,
    escapeinside={(*@}{@*)}
}

% 论文信息
\title{\textbf{\Large 基于控制混杂偏相关与随机报童模型的蔬菜自动定价与补货策略优化}}
\tihao{C}
\baominghao{245306245276245173}
\schoolname{天津大学}
\membera{任欣雨（245306）}
\memberb{刁雨欣（245276）}
\memberc{于孜（245173）}
\supervisor{}
\yearinput{2026}
\monthinput{7}
\dayinput{21}

\begin{document}



\begin{abstract}
\begin{center}
{\heiti\Large\bfseries 基于控制混杂偏相关与随机报童模型的蔬菜自动定价与补货策略优化}
\end{center}
\vspace{1em}


本文针对生鲜商超蔬菜类商品的自动定价与补货决策问题，通过对历史销售流水、批发价格与损耗率等数据的深度挖掘与量化分析，建立了一系列运筹优化与数据驱动模型，为商超的精细化运营提供了科学严谨的决策依据。

\textbf{针对问题一}，本文探究了各蔬菜品类及单品销量的分布规律与相关关系。首先，通过K-S检验验证了各类蔬菜销量呈现显著的非正态右偏特征，并绘制了时序波动图揭示其季节性规律。随后，为了克服传统皮尔逊系数的局限性，本文引入\textbf{斯皮尔曼等级相关系数}，精准刻画了各品类及核心单品之间的替代与互补属性。最后，采用\textbf{Apriori算法}进行购物篮关联规则挖掘，识别出了如“花叶类与调味配菜”等高置信度的商品组合，为后续的关联陈列提供了数据支撑。

\textbf{针对问题二}，本文在总销量空间受限及未来批发价已知的条件下，制定了商超各品类的最优定价与补货策略。首先，采用\textbf{温特斯季节模型}预测各品类基准销量，并基于预测基准和历史均价构建\textbf{线性校准需求函数}（弹性系数取-1.2）。在此基础上，引入考虑损耗率的利润函数，并采用\textbf{模拟退火算法}对每个品类逐日独立进行全局寻优，在给定边界约束下联合优化补货量与定价，有效平衡了缺货损失与滞销折损，求得未来一周的单日最优品类组合策略。

\textbf{针对问题三}，本文将决策粒度细化至单品级别，并引入了“最小陈列量限制（2.5千克）”与“可售SKU总量控制（27-33个）”等硬性离散约束。采用\textbf{两阶段降维求解法}：第一阶段通过\textbf{0-1整数线性规划（intlinprog）}筛选高收益单品组合；第二阶段以\textbf{连续非线性规划（fmincon）}优化定价与补货量，并纳入资金约束（3000元），最终给出兼顾“高溢价撇脂”与“平价走量”策略的最佳单日方案。

\textbf{针对问题四}，本文回顾了前期建模局限与真实的线下零售业务场景，指出了现有数据在时序颗粒度与外部宏观控制变量上的缺失。通过\textbf{灰色关联分析}量化验证了品种丰富度、节气特征、节日特征与销量的强关联性，进而建议商超增设分时段销售流水、单品品相质检、客流量与环境气象等增量数据采集体系，以提升模型的动态适应性与预测鲁棒性。

\keywords{灰色关联度分析 \quad 关联规则挖掘 \quad 报童模型 \quad 整数线性规划 \quad 序列二次规划}
\end{abstract}

% 目录（正式比赛可不加）
% \tableofcontents

\section{问题重述}

\subsection{问题背景}
生鲜商超中，蔬菜类商品是吸引客流的重要品类，但也面临着保鲜期短、品相随时间下降、损耗率高等问题。为了避免货物变质积压，商超通常需要在营业时间内根据销售情况进行动态打折。同时，商超需要在每天营业结束后，根据当天的销售情况、历史数据和次日的批发价，来制定次日的补货计划和定价策略。由于蔬菜品种繁多、产地和气候条件各不相同，且同一品类下不同单品之间可能存在替代或互补关系，传统的基于人工经验的补货和定价方式不仅效率低下，而且难以实现收益最大化。因此，如何利用历史销售数据，建立数学模型，实现蔬菜类商品的自动定价与补货决策，是商超运营中亟待解决的关键问题。

\subsection{问题的提出}
基于上述背景，本文需要解决以下四个问题：

\begin{enumerate}
    \item \textbf{数据探索与关联分析}：深入分析附件中的蔬菜销售流水数据，探索各蔬菜品类及单品销售量的分布规律，并挖掘不同蔬菜品类之间、以及不同单品之间的关联关系，为后续定价与补货建模提供数据基础。

    \item \textbf{品类级定价与补货联合优化}：假设商超每天能提供的各品类蔬菜总销售空间有限，结合各品类的历史销售数据、批发价和损耗率，建立数学模型，分别给出未来一周（2023年7月1-7日）各蔬菜品类的每日总补货量和定价策略，以实现商超收益最大化。

    \item \textbf{单品级选品与定价组合优化}：在前一问题的基础上进一步考虑单品级别的决策。由于单品种类繁多，商超要求每天售卖的单品总数应控制在特定范围内（如27-33个），且每个被选中的单品需要满足最小陈列量要求（如2.5千克），需建立模型给出未来一天（2023年7月1日）各个具体单品的补货量和定价策略，以最大化当天收益。

    \item \textbf{模型评价与数据拓展}：探讨为了进一步提升自动定价与补货决策模型的准确性和实用性，商超还需要采集哪些相关数据，说明其对解决上述问题的帮助，并提出具体的数据采集方案。
\end{enumerate}

\section{问题的分析}
本题的核心目标是基于某商超蔬菜类商品的历史销售流水、批发价格和损耗率等数据，建立一套科学的自动定价与补货决策模型。商超的生鲜经营面临着保鲜期短、品相随时间下降以及消费者对价格敏感等复杂因素。为了最大化商超的整体利润，我们需要从宏观的品类关联规律逐步深入到微观的单品时序预测，并结合有限的展台空间和陈列量约束进行组合优化。整体建模思路遵循“数据探索 $\to$ 品类级优化 $\to$ 单品级组合优化 $\to$ 模型评价与拓展”的递进逻辑。

\subsection{问题一的分析}
问题一要求探索蔬菜品类及单品销量的分布规律与相互关联，是一个典型的数据挖掘与探索性数据分析问题。首先，需要对原始销售流水进行深度清洗，提取出日均销量、时间趋势和损耗特征，从宏观上把握各大品类的结构差异。考虑到销量数据可能不服从正态分布，需要采用K-S检验验证分布特征，并据此选择非参数的相关性分析方法。其次，为了找出不同蔬菜之间的组合购买行为，需要利用\textbf{斯皮尔曼相关系数}分析品类及单品间的替代与互补关系，再采用\textbf{Apriori关联规则挖掘算法}寻找潜藏在购物篮数据中的强关联规则，为后续的商品组合摆放与捆绑销售策略提供数据支撑。

\subsection{问题二的分析}
问题二要求在已知各品类历史数据和总销售空间受限的前提下，制定未来一周各品类的总补货量和定价策略以最大化商超收益，属于带有边界约束的非线性连续优化问题。由于销量不仅受时间周期性影响，还随定价的变动而呈现负相关，因此需要分两步处理：首先采用\textbf{温特斯（Holt-Winters）季节模型}对各品类未来一周的销量进行预测，以周（7天）为季节周期，对多数品类使用加法模型、对花叶类使用乘法模型，得到每日基准销量 $D_{\text{base}}$；随后，以该基准销量为锚点，结合各品类历史平均售价 $P_{\text{avg}}$，构造线性需求校准函数
\[
D(P) = D_{\text{base}} + \varepsilon \cdot \frac{D_{\text{base}}}{P_{\text{avg}}}\, (P_{\text{avg}} - P),
\]
其中 $\varepsilon=-1.2$ 为预设的价格弹性（生鲜产品典型值），确保在历史均价附近需求与预测值一致，且满足需求定律。

在优化层面，将各品类视为独立的决策单元，以单日期望利润最大化为目标。利润函数同时考虑销售收入、进货成本（以逐日批发价 $W$ 计）以及未售出部分按损耗率 $\theta$ 承担的损失：
\[
\pi(Q,P) = \min(Q,D(P))\cdot P - \min(Q,D(P))\cdot W - \max(Q-D(P),0)\cdot W\cdot\theta.
\]
由于该利润函数关于定价 $P$ 和补货量 $Q$ 呈现非凸、多峰特性，且决策变量间存在耦合，本文采用\textbf{模拟退火算法（Simulated Annealing）}进行全局寻优。对每个品类、未来每一天独立求解，设置定价上下界（批发价的1.05倍至历史均价的1.15倍及批发价1.8倍的较小值）和补货量上下界（基准销量的0.3倍至1.5～2.5倍），以高效逼近全局最优。该方法不依赖于梯度信息，能够稳健应对利润函数的多峰性，最终输出未来一周（2023年7月1-7日）每个品类每日的最优补货量和定价策略。

\subsection{问题三的分析}
问题三在问题二的基础上进一步细化，要求制定单品级别的补货和定价策略，并引入了“可售单品总数限制（27-33个）”和“最小陈列量（2.5千克）”等硬性离散约束。这使得原问题转变为包含0-1选品变量和连续定价变量的混合优化问题，属于\textbf{混合整数非线性规划（MINLP）}。为了降低求解难度，采用\textbf{两阶段降维策略}：第一阶段忽略单品间交互，以各单品独立最优利润为目标，构建\textbf{0-1整数线性规划}求解最优选品组合；第二阶段在固定选品基础上，用\textbf{连续非线性规划（SQP，fmincon）}联合优化各单品的定价，并纳入资金预算约束（3000元）。这种分解方法既能保证全局可行性，又能利用成熟求解器获得高质量解。

\subsection{问题四的分析}
问题四属于开放性的模型评价与数据拓展问题，要求为了更好地实现自动定价与补货，提出商超还需要采集哪些数据。这需要反思前期建模过程中的痛点与信息缺失：例如，现有模型默认了一日内的固定价格，与商超晚上打折出清的实际情况不符；缺乏客流和天气数据，难以量化外部环境的干扰；损耗率使用历史均值，无法反映批次间的品质差异。因此，需要引入\textbf{灰色关联分析}，利用已有数据量化验证品种丰富度、节气特征、节日特征等外部因素与销量的关联强度，据此提出有针对性的数据采集方案，为模型的升级迭代指明方向。

\section{模型假设}
为了使模型更贴近实际并易于求解，本文提出如下假设：
\begin{enumerate}
    \item 假设历史销售流水数据中的退货记录已被合理剔除或修正，剩余数据均反映真实的销售情况。
    \item 假设附件提供的每日批发价在一天内保持稳定，不随时间发生剧烈波动，且蔬菜的损耗率是一个稳定的常数，不随天气等外部短期因素产生大幅改变。
    \item 假设消费者对蔬菜的需求主要受价格影响，服从一定的线性需求弹性函数。
    \item 假设商超不会出现严重的供应链中断，即只要商超下单，供应商都能足量供货。
\end{enumerate}

\section{符号说明}
\begin{table}[H]
\centering
\caption{主要符号说明}
\begin{tabular}{cp{8cm}}
\toprule
符号 & 意义 \\
\midrule
$i$ & 蔬菜品类或单品索引 \\
$t$ & 时间（天） \\
$Q_{i,t}$ & 第$i$种蔬菜在第$t$天的补货量（千克） \\
$P_{i,t}$ & 第$i$种蔬菜在第$t$天的销售定价（元/千克） \\
$C_{i,t}$ & 第$i$种蔬菜在第$t$天的批发进价（元/千克） \\
$D_{i,t}$ & 第$i$种蔬菜在第$t$天的实际需求量（千克） \\
$\delta_i$ & 第$i$种蔬菜的平均损耗率 \\
$R$ & 商超的每日总收益（元） \\
\bottomrule
\end{tabular}
\end{table}

\section{数据预处理}
原始流水数据中存在部分异常记录。我们先按逻辑校验剔除了销量和售价小于等于0的无效流水。之后将商品分类表、批发价与损耗率按日期和单品编码做左连接合并，聚合为“品类-日”和“单品-日”两个层级的面板数据。对于极少数缺失的批发价，考虑到农产品价格时间连续性强，我们用相邻几日的移动均值做了插补。

\subsection{销量分布的统计特征}
以销量最大的“花叶类”为例，我们绘制了日销量频数直方图，并用正态分布和对数正态分布做了拟合，如图\ref{fig:dist_fit}~所示。销量分布呈现明显的右偏特征，即多数天数销量集中在较低水平，少数天数因促销或节假日出现异常高销量，这一特征将直接影响后续需求建模的分布假设选择。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{图1.png}
    \caption{花叶类日销量分布规律拟合图}
    \label{fig:dist_fit}
\end{figure}

从拟合结果可以明显看出，销量图有很长的“右尾巴”，不是对称的钟形曲线。为了量化这个特征，我们计算了各品类的均值、中位数、偏度、峰度，并做了K-S拟合优度检验，结果见表\ref{tab:stats}。

\begin{table}[H]
\centering
\caption{各品类日均销量核心统计量汇总表}
\label{tab:stats}
\begin{tabular}{lcccccc}
\toprule
品类 & 均值 (kg) & 标准差 & 中位数 & 偏度 & 峰度 & K-S检验 $p$ \\
\midrule
花叶类 & 183.10 & 86.30 & 173.21 & 2.89 & 28.98 & $<10^{-10}$ \\
花菜类 & 84.47 & 53.46 & 72.93 & 3.14 & 21.31 & $<10^{-10}$ \\
食用菌 & 70.17 & 48.52 & 57.54 & 2.99 & 19.80 & $<10^{-10}$ \\
茄类 & 38.55 & 22.69 & 34.07 & 1.52 & 7.18 & $<10^{-4}$ \\
水生根茎类 & 37.43 & 31.37 & 30.29 & 2.50 & 15.08 & $<10^{-7}$ \\
辣椒类 & 21.37 & 13.16 & 18.87 & 1.74 & 8.86 & $<10^{-5}$ \\
\bottomrule
\end{tabular}
\end{table}

从表\ref{tab:stats}可以看出，所有品类的偏度都远大于0（尤其在1.5以上），峰度远大于3，而且正态性检验的$p$值都趋近于0。据此可以确定，蔬菜销量严重不服从正态分布，而明显更接近对数正态分布。这个结论决定了后续做相关性分析时不能直接用Pearson系数，也决定了问题二中报童模型中对需求分布应采用对数正态假设。

\section{问题一：品类与单品关联关系分析}
我们分三个层次展开分析：先看各品类的销售体量和损耗风险，再看品类和单品之间的替代、互补关系，最后用购物篮分析找具体的搭配规律。

\subsection{宏观销售特征与损耗风险}
\subsubsection{销量占比}
图\ref{fig:pct}展示了各品类在总销量中的贡献度分布。从图中可以看出，花菜类以39.8\%的占比位居第一，花叶类以28.7\%紧随其后，两者合计占比接近七成。食用菌（7.6\%）、辣椒类（17.9\%）等品类占比相对较小。这一结构表明，在后续制定补货策略时，花菜类和花叶类的供给稳定性需要优先保证，它们的库存波动会直接左右整体营收。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{2.jpg}
    \caption{各品类销量占比分布图}
    \label{fig:pct}
\end{figure}

图\ref{fig:pct}直观反映了各品类的销售结构，花菜类和花叶类合计占比超过68\%，是商超蔬菜经营的核心品类，其补货策略的制定应优先保障，避免缺货对整体营收造成冲击。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{3.png}
    \caption{各品类季度平均销量时间趋势图}
    \label{fig:trend}
\end{figure}

从图\ref{fig:trend}可以观察到所有品类的销量都存在明显的季节波动，没有一年四季固定不变的规律。尤其值得注意的是花叶类，其波峰与波谷之间的落差极大，第三季度销量可低至约25，而第四季度可高达约140，波动幅度接近6倍，这和它的保鲜期短、容易受气候影响有关。相比之下，茄类波动相对平缓，全年销量维持在30-60之间。水生根茎类在每年秋冬（第四季度）出现独立的小高峰，与花叶类形成了一定程度的错峰。这一发现提示：不同品类的补货策略必须随季节动态差异化调整，不能简单按年平均水平来统一制定。

为了更清晰地比较各品类的波动幅度差异，我们进一步将各品类销量进行归一化处理（以各自均值为基准），如图\ref{fig:norm_trend}所示。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{4.png}
    \caption{各品类销量归一化趋势对比图}
    \label{fig:norm_trend}
\end{figure}

图\ref{fig:norm_trend}揭示了一个重要规律：花叶类和花菜类的标准化波动幅度远大于其他品类，其销量在旺季可达淡季的2倍以上，属于“高波动型”品类；而茄类和辣椒类的波动相对平缓，属于“稳定型”品类。这一差异意味着：对于高波动品类，商超需要更精准的短期需求预测和更灵活的补货策略（如小批量多频次补货），而对于稳定型品类，则可以适当简化预测方法，降低管理成本。

\subsubsection{损耗率差异}
图\ref{fig:loss}对比了六大品类的平均损耗率，数据来源于附件4中的品类级损耗率统计。从图中可以清晰地看到品类的损耗率呈现出明显分化：花菜类以15.51\%高居首位，水生根茎类（13.65\%）和花叶类（12.83\%）紧随其后，而茄类的损耗率最低，仅为6.68\%。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{5.png}
    \caption{各品类平均损耗率对比图}
    \label{fig:loss}
\end{figure}

结合前面的销量占比分析（图\ref{fig:pct}），花叶类既是销量大头（28.7\%），又是损耗重灾区（12.83\%），两者叠加造成的绝对金额损失是最大的。花菜类虽然销量占比最高（39.8\%），但损耗率也最高（15.51\%），同样面临“高销量、高损耗”的双重压力。这一发现为后续问题二建模提供了关键输入参数：在构建报童模型时，花叶类和花菜类的滞销风险必须用高损耗率准确地折算进成本函数中，否则模型将严重低估实际损失。

\subsection{斯皮尔曼相关性分析}
前面已经通过K-S检验证明销量不服从正态分布，因此不能使用基于正态假设的Pearson相关系数。我们改用非参数的Spearman秩相关系数来分析品类和单品之间的关联性。

图\ref{fig:spearman_cat}展示了六大品类的Spearman相关矩阵热力图。从图中可以观察到：花叶类、花菜类、水生根茎类之间的相关系数都在0.4以上，表明它们经常被消费者同时购买，属于互补型品类组合。而茄类和辣椒类之间呈现负相关关系（相关系数约为-0.2），说明消费者在采购时倾向于在这两者中做取舍，属于替代型品类组合。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{6 .png}
    \caption{六大品类Spearman相关性热力图}
    \label{fig:spearman_cat}
\end{figure}

进一步地，我们计算了历史销量Top15的单品Spearman相关矩阵，如图\ref{fig:spearman_item}所示。微观层面的结果对货架摆放具有直接参考价值：散装云南生菜和按份售卖的云南生菜相关系数为$-0.75$，属于强替代关系，说明同一种蔬菜没有必要同时上架两种规格，这会互相挤占销量。相反，小米椒与云南生菜、油麦菜等绿叶菜的相关系数都在0.8以上，属于强互补关系——小米椒是典型的调味配菜，消费者购买时通常会搭配主菜（绿叶菜）一起购买。据此，商超可将互补品类放置在相邻货架，以提升连带购买率。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{7.png}
    \caption{Top15单品Spearman相关性热力图}
    \label{fig:spearman_item}
\end{figure}

\subsection{Apriori购物篮关联规则挖掘}
Spearman相关分析反映的是销量波动在时序上的同步性，但为了直接获取“一次交易中哪些蔬菜会被同时购买”的结论，我们进一步将流水数据整理为购物篮事务数据，采用Apriori算法进行关联规则挖掘。设定最小支持度为0.05（即规则覆盖至少5\%的交易记录），最小置信度为0.5（即前件出现时后件出现的概率不低于50\%）。

表\ref{tab:apriori}列出了满足阈值条件的Top5强关联规则。提升度（Lift）是衡量规则有效性的关键指标，当提升度大于1时，说明前件与后件的共现并非偶然，而是具有实际的消费逻辑关联。

\begin{table}[H]
\centering
\caption{Apriori强关联规则（Top5）}
\label{tab:apriori}
\begin{tabular}{lcccc}
\toprule
前件 & 后件 & 支持度 & 置信度 & 提升度 \\
\midrule
小米椒（份） & 云南生菜（份） & 0.112 & 0.86 & 1.44 \\
大白菜 & 青梗散花 & 0.098 & 0.82 & 1.48 \\
散装云南生菜 & 油麦菜（份） & 0.087 & 0.71 & 1.35 \\
金针菇 & 上海青（份） & 0.069 & 0.65 & 1.18 \\
青尖椒 & 螺丝椒 & 0.052 & 0.59 & 2.02 \\
\bottomrule
\end{tabular}
\end{table}

从表\ref{tab:apriori}可以看出，所有规则的提升度均大于1，最高的是“青尖椒→螺丝椒”，提升度达到2.02，说明这两种辣椒几乎总是被同时购买。结合前述Spearman分析中“小米椒与绿叶菜”的强互补关系，Apriori挖掘为具体的陈列优化提供了更明确的落地方案：建议在绿叶菜区域设置一个“配菜专区”，集中陈列小米椒、金针菇等高频配菜；青尖椒和螺丝椒则可按烹饪场景（如“辣椒组合”）进行组合陈列，以方便消费者一站式购齐。

\subsection{需求特征总结与模型选型依据}
上述探索性分析为后续建模奠定了两个核心基础：第一，销量严重右偏且受价格调控影响大，因此后续需求函数不适合采用普通线性回归，而应采用对数-线性或线性化需求弹性模型，并在报童框架中假设需求服从对数正态分布；第二，花叶类、花菜类等高损耗品类贡献了销量大头，补货量过多会导致滞销损耗激增，补货量不足又会造成缺货损失，因此必须引入能够精确平衡缺货与滞销成本的报童模型。问题二将在此框架下，把各个品类视为独立的需求单元，先进行宏观层面的最优资源分配与定价求解；问题三再进一步细化到单品层面，并处理组合选择约束。

\section{问题二：品类级定价与补货联合优化}

\subsection{优化模型建立}

基于问题一的探索性分析，我们将每个蔬菜品类视为独立的报童决策单元。在给定未来一周各品类的批发价预测值的前提下，需要同时确定每个品类每日的销售定价和补货量，以最大化商超的总期望利润。

\subsubsection{决策变量}

对于第$t$天（$t=1,\dots,7$）的第$c$个品类（$c=1,\dots,6$），定义：
对于第 $t$ 天（$t=1,\dots,7$）的第 $c$ 个品类（$c=1,\dots,6$），定义的决策变量如表 \ref{tab:p2_vars} 所示。

\begin{table}[htbp]
\centering
\caption{品类级优化模型决策变量定义}
\label{tab:p2_vars}
\begin{tabular}{c c c l}
\toprule
\textbf{决策变量} & \textbf{取值范围} & \textbf{物理单位} & \textbf{决策含义与变量性质} \\
\midrule
$P_{c,t}$ & $\mathbb{R}^+$ & 元/kg & 第 $t$ 天第 $c$ 个品类的销售定价（连续变量） \\
$Q_{c,t}$ & $\mathbb{R}^+$ & kg & 第 $t$ 天第 $c$ 个品类的采购补货量（连续变量） \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{需求函数}

根据问题一的结论，销量受价格影响显著且呈现非线性特征，我们采用线性校准需求函数：
\begin{equation}
D_{c,t}(P) = D_{c,t}^{\text{base}} + \varepsilon \cdot \frac{D_{c,t}^{\text{base}}}{\bar{P}_c} (\bar{P}_c - P)
\end{equation}
其中$D_{c,t}^{\text{base}}$为温特斯季节模型预测的基准销量，$\bar{P}_c$为该品类的历史平均售价，$\varepsilon=-1.2$为预设价格弹性系数。该形式确保在历史均价附近需求与预测一致，并满足需求定律。

\subsubsection{利润函数}

以单日期望利润最大化为目标。实际销量为 $\min(Q_{c,t}, D_{c,t}(P))$，未售出部分按损耗率 $\delta_c$ 承担损失。单品类利润函数为：
\begin{equation}
\pi_{c,t}(Q,P) = \min(Q,D(P))\cdot P - \min(Q,D(P))\cdot C_{c,t} - \max(Q-D(P),0)\cdot C_{c,t}\cdot\delta_c
\end{equation}
其中$C_{c,t}$为第$c$品类在第$t$天的批发进价。总期望利润为各品类利润之和。

\subsubsection{边界约束}

为保证解的可行性与求解稳健性，对各品类设定如下边界约束：
为保证解的可行性与求解稳健性，对各品类设定如表 \ref{tab:p2_bounds} 所示的边界约束。

\begin{table}[htbp]
\centering
\caption{品类级优化模型边界约束设置}
\label{tab:p2_bounds}
\begin{tabular}{c l l}
\toprule
\textbf{约束类型} & \textbf{数学表达式} & \textbf{业务设定依据与物理含义} \\
\midrule
定价下限 & $P_{\text{lb}} = 1.05 \cdot C_{c,t}$ & 确保售价高于进货成本并保障 5\% 基本毛利 \\
定价上限 & $P_{\text{ub}} = \min(1.15\bar{P}_c,\ 1.8C_{c,t})$ & 防范过度溢价引发客户流失与销量下滑 \\
补货量下限 & $Q_{\text{lb}} = 0.3 D_{c,t}^{\text{base}}$ & 保障品类货架基本陈列与展示需求 \\
补货量上限 & $Q_{\text{ub}} = k_c \cdot D_{c,t}^{\text{base}}$ & $k_c=2.0$（花叶类），$k_c=3.5$（其余品类） \\
\bottomrule
\end{tabular}
\end{table}


\subsection{求解算法}
由于利润函数关于$P$和$Q$呈非凸、多峰特性，解析求解困难，本文采用\textbf{模拟退火算法}进行全局优化。对每个品类、每一天独立运行，算法从较高初始温度（$T_{\text{init}}=500$）开始，以正态扰动生成邻域解，采用Metropolis准则接受较差解以跳出局部最优，温度按系数$0.98$衰减至$10^{-6}$收敛，内层迭代次数为400。最终输出全局近似最优解。

\subsection{结果分析与策略解读}
采用模拟退火算法对每个品类每一天独立优化后，得到未来一周的最优定价与补货策略，如图\ref{fig:result}所示。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{8.png}
    \caption{2023年7月1日至7日各品类最优补货量与定价策略}
    \label{fig:result}
\end{figure}

图\ref{fig:result}展示了未来一周六个品类的逐日最优补货量和定价水平。从补货量来看，花叶类每日补货量稳定在150kg左右，远高于其他品类，这是由其高销量基数决定的。但从定价水平来看，花叶类的定价仅为3.67元/kg，加价率约72\%，低于辣椒类的119.78\%和茄类的99.94\%。这表明模型为花叶类选择了“低毛利、高周转”的策略，通过相对低廉的价格加速销售，减少高损耗带来的滞销损失。

表\ref{tab:day1}汇总了2023年7月1日（周一）各品类的详细决策结果。

\begin{table}[H]
\centering
\caption{7月1日各品类定价与补货策略}
\label{tab:day1}
\begin{tabular}{lcccc}
\toprule
品类 & 最优定价(元/kg) & 预测销量(kg) & 加价率 & 最优补货量(kg) \\
\midrule
花菜类 & 11.72 & 20.78 & 77.38\% & 5.50 \\
花叶类 & 3.67 & 6.32 & 72.10\% & 149.90 \\
辣椒类 & 7.79 & 17.12 & 119.78\% & 18.72 \\
茄类 & 4.38 & 8.76 & 99.94\% & 17.94 \\
食用菌 & 5.96 & 9.69 & 62.75\% & 60.78 \\
水生根茎类 & 4.62 & 9.13 & 97.66\% & 56.57 \\
\bottomrule
\end{tabular}
\end{table}

结合表\ref{tab:day1}的优化求解结果，商超对不同蔬菜品类形成了清晰的差异化运营策略。

第一，花叶类采取“高周转、低溢价”的引流策略。该品类平均定价为3.67元/kg，加价率设为72.10\%，日均补货量达149.90kg。由于花叶类具备较强的价格弹性，保持低价能够有效拉动客流并加速出清，从而显著降低其高达12.83\%的滞销损耗风险。

第二，辣椒类与茄类承担“高毛利、低库存”的利润贡献角色。两者的平均定价分别达到7.79元/kg和4.38元/kg，加价率高达119.78\%和99.94\%，日补货量维持在18kg左右。此类调味与烹饪刚需蔬菜的市场需求弹性相对较低，适当提高溢价率可最大化单件商品边际利润。

第三，花菜类、食用菌与水生根茎类展现“平衡型”稳健特征。其定价与补货规模均处于中等水平，加价率保持在62\%至98\%之间，在维持货架丰富度与留存商超综合利润之间达成了动态平衡。

在未来一周内，该模型求得的总期望利润为4392.18元（日均627.45元），验证了差异化品类定价策略在平衡缺货损失与滞销折损上的显著有效性。

\section{问题三：单品级定价与补货组合优化}

问题二将六大品类视为独立的决策单元，解决了宏观层面的资源分配问题。但现实中商超的实际运营是以具体单品（SKU）为单位进行采购和陈列的。同一品类下的不同单品之间往往存在替代关系（如两种规格的云南生菜会互相抢客），同时商超还有硬性的业务约束：当日上架的单品总数必须控制在27-33个之间，且每个被选中的单品最小陈列量不得低于2.5千克。这些问题将原本的连续优化问题转变为“选哪些单品 + 分别定什么价 + 补多少货”的组合优化问题。

\subsection{优化模型建立}

\subsubsection{候选单品集合}

选取2023年6月最后一周（6月24日至30日）有销售记录的活跃单品作为候选集，共$N=49$个单品。候选单品信息包括：单品编码、所属品类、历史日均销量$D_i^0$、历史平均售价$\bar{P}_i$、批发进价$W_i$（按$0.65\bar{P}_i$估算）、损耗率$\delta_i$（由其所属品类的平均损耗率赋值）。

\subsubsection{决策变量}

定义三类决策变量：
单品级模型包含选品、定价与采购量三重决策，定义的决策变量如表 \ref{tab:p3_vars} 所示。

\begin{table}[htbp]
\centering
\caption{单品级 MINLP 优化模型决策变量定义}
\label{tab:p3_vars}
\begin{tabular}{c c c l}
\toprule
\textbf{决策变量} & \textbf{取值范围} & \textbf{物理单位} & \textbf{决策含义与变量性质} \\
\midrule
$x_i$ & $\{0, 1\}$ & -- & 第 $i$ 个单品是否选中上架（0-1 离散变量） \\
$P_i$ & $\mathbb{R}^+$ & 元/kg & 第 $i$ 个单品的销售定价（连续变量） \\
$Q_i$ & $\mathbb{R}^+$ & kg & 第 $i$ 个单品的采购补货量（连续变量） \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{单品需求与利润函数}

与问题二类似，单品$i$的需求函数为线性校准形式：
\begin{equation}
D_i(P_i) = D_i^0 + \varepsilon \cdot \frac{D_i^0}{\bar{P}_i} (P_i - \bar{P}_i)
\end{equation}
其中$\varepsilon = -1.2$为预设价格弹性。

补货量$Q_i$需同时满足最小陈列量约束和需求覆盖约束：
\begin{equation}
Q_i(P_i) = \max\left(Q_{\min},\ \frac{D_i(P_i)}{1-\delta_i}\right)
\end{equation}
其中$Q_{\min}=2.5$千克为最小陈列量下限。

单品利润函数为：
\begin{equation}
\pi_i(P_i) = P_i \cdot \min\bigl(D_i(P_i),\ Q_i(P_i)(1-\delta_i)\bigr) - W_i \cdot Q_i(P_i)
\end{equation}

\subsubsection{完整优化模型}

综合以上变量和函数，单品级定价与补货组合优化的完整数学模型表述如下：

\begin{equation}
\max_{x_i,\ P_i,\ Q_i} \quad \sum_{i=1}^{N} x_i \cdot \pi_i(P_i)
\end{equation}

约束条件体系如式 (15) 所示：
\begin{equation}
\text{s.t.} \begin{cases}
27 \le \sum_{i=1}^{N} x_i \le 33 & (\text{1. 上架单品总数约束}) \\[6pt]
\sum_{i \in \mathcal{C}_k} x_i \ge 1,\quad \forall k=1,\dots,6 & (\text{2. 品类多样性覆盖约束}) \\[6pt]
Q_i = \max\left(Q_{\min},\ \frac{D_i^0 + \varepsilon \cdot \frac{D_i^0}{\bar{P}_i} (P_i - \bar{P}_i)}{1-\delta_i}\right) \cdot x_i & (\text{3. 补货量与定价联动约束}) \\[6pt]
\frac{W_i}{1-\delta_i} + 0.01 \le P_i \le \max(1.5\bar{P}_i,\ 2.0W_i) & (\text{4. 保本与溢价上下限约束}) \\[6pt]
\sum_{i=1}^{N} W_i \cdot Q_i \le 3000 & (\text{5. 资金预算上限约束}) \\[6pt]
x_i \in \{0,1\},\quad \forall i=1,\dots,N & (\text{6. 0-1 变量决策约束})
\end{cases}
\end{equation}

上述各项约束条件的实际业务背景解释如下：第一，\textbf{上架单品总数约束}限制每日上架销售的单品总数在 $27 \sim 33$ 个之间，以匹配货架陈列空间；第二，\textbf{品类多样性覆盖约束}保障 6 大蔬菜品类中每个品类至少有 1 个单品上架（$\mathcal{C}_k$ 表示第 $k$ 个品类的单品集合）；第三，\textbf{补货量与定价联动约束}使采购量受价格弹性调控并满足单品最小陈列量 $Q_{\min} = 2.5$ kg 下限；第四，\textbf{保本与溢价上下限约束}保障售价高于采购成本并防范过度溢价；第五，\textbf{资金预算上限约束}限制全场进货总成本不超过 3000 元上限；第六，\textbf{0-1 变量决策约束}明确选品变量 $x_i \in \{0,1\}$ 的离散二值属性。

该模型是一个典型的混合整数非线性规划（Mixed-Integer Nonlinear Programming, MINLP），其难点在于0-1选品变量与连续定价变量的耦合，以及非凸的目标函数形式。直接求解该模型在$N=49$的规模下虽然可行，但计算效率较低。为降低求解难度，我们采用下文的两阶段降维策略进行高效求解。

\subsection{两阶段求解方法}

由于上述MINLP模型直接求解困难，我们设计两阶段降维求解策略：

\subsubsection{阶段一：单品独立最优定价与0-1整数线性规划选品}

首先忽略单品间交互效应，对每个候选单品独立求解其最优定价$P_i^*$和对应最大利润$\pi_i^*$：
\begin{equation}
P_i^* = \arg\max_{P_i} \ \pi_i(P_i)
\end{equation}
采用黄金分割搜索（`fminbnd`）对每个单品独立求解。然后将$\pi_i^*$作为候选单品的利润系数，构建0-1整数线性规划选品模型：
\begin{equation}
\max_{x_i} \quad \sum_{i=1}^{N} \pi_i^* \cdot x_i
\end{equation}
约束条件为：
\begin{equation}
27 \leq \sum_{i=1}^{N} x_i \leq 33,\quad \sum_{i \in \mathcal{C}_k} x_i \geq 1\ (\forall k)
\end{equation}
该模型为标准的0-1整数线性规划，采用`intlinprog`精确求解。

\subsubsection{阶段二：连续非线性规划联合定价优化}

阶段一得到最优选品组合（假设选出$M$个单品，$27 \leq M \leq 33$），但阶段一中各单品的定价是独立求解的，并未考虑单品间的潜在替代关系以及资金预算约束。因此，在固定选品集合的基础上，对定价进行联合优化：

\begin{equation}
\max_{\mathbf{P}} \quad \sum_{k=1}^{M} \pi_k(P_k)
\end{equation}
其中$\mathbf{P} = (P_1,\dots,P_M)$为$M$维连续定价向量。约束条件包括各单品的定价上下限以及资金预算约束：
\begin{equation}
\sum_{k=1}^{M} W_k \cdot Q_k(P_k) \leq 3000 \text{ 元}
\end{equation}
该问题为带有非线性不等式约束的连续优化问题，采用序列二次规划（SQP，`fmincon`）求解，初始点设为阶段一得到的各单品独立最优定价。

\subsection{选品结果与淘汰逻辑分析}

两阶段求解完成后，模型共选出33个单品（恰好达到上限），总建议补货量为265.30千克，预计单日毛利约为1433.95元，总进货成本严格控制在3000元预算以内。

表\ref{tab:category_dist}展示了被选中的33个单品按品类分布情况。

\begin{table}[H]
\centering
\caption{选中单品品类分布统计}
\label{tab:category_dist}
\begin{tabular}{lccc}
\toprule
品类 & 选中单品数 & 合计利润贡献（元） & 平均利润/单品（元） \\
\midrule
花叶类 & 11 & 443.02 & 40.27 \\
花菜类 & 2 & 157.17 & 78.58 \\
水生根茎类 & 4 & 194.67 & 48.67 \\
茄类 & 3 & 103.49 & 34.50 \\
辣椒类 & 8 & 359.02 & 44.88 \\
食用菌 & 5 & 176.58 & 35.32 \\
\hline
合计 & 33 & 1433.95 & 43.45 \\
\bottomrule
\end{tabular}
\end{table}

从表\ref{tab:category_dist}可以看出，辣椒类选中8个单品，贡献利润359.02元（占比约25\%），是利润贡献最大的品类；花菜类虽然仅选中2个单品，但平均利润/单品高达78.58元，是平均利润率最高的品类，这与问题二中“花菜类高损耗、高加价”的策略定位一致。花叶类选中11个单品（最多），合计利润443.02元（占比约31\%），体现了其“以量取胜”的引流定位。这一品类分布结果与问题二差异化策略一脉相承。

图\ref{fig:profit_rank}展示了选中33个单品按利润贡献从高到低的排序情况。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{9.png}
    \caption{选中33个蔬菜单品利润贡献排名图}
    \label{fig:profit_rank}
\end{figure}

从图\ref{fig:profit_rank}可以观察到，利润贡献排名前10的单品贡献了总利润的约55\%，呈现明显的“长尾分布”特征。芜湖青椒、小米椒等单品排名靠前，表明弹性大、客单价适中的单品是利润的主要来源。

未被选入最优组合的16个单品（占候选总数的32.7\%）的淘汰原因可归结为以下两类。

第一类为低弹性、低销量型单品。这类单品（如某些冷门的菌菇品种）的价格弹性系数$|\varepsilon_i|$极小，即使大幅降价也难以拉动销量，同时在历史数据中日均销量本就低迷（低于1千克）。按照阶段一的独立最优定价计算，其$\pi_i^*$极低甚至为负（即进货即亏损），在0-1整数规划的目标函数中自然被排除。强行上架这类单品不仅贡献不了利润，还容易因跨不过2.5千克的最低陈列量而产生积压变质。

第二类为高进价、低溢价空间型单品。这类单品的批发进价$W_i$偏高，导致在定价上限约束下加价率有限（无法达到品类平均加价水平），即使销量尚可，其单位利润也远低于同类替代品。在实际销售中，消费者对价格较为敏感，高价单品很容易被同一品类中定价更低的同类单品挤占市场份额。

从定价和补货策略来看，在选定的33个单品中，定价与补货量呈现明显的差异化规律：芜湖青椒、小米椒等弹性大、客单价适中的单品，模型采用了中等定价、较高补货量的策略，充分发挥其引流和利润双重作用；而部分高端菌菇单品，模型则采用了高定价、补货量紧贴2.5千克下限的“撇脂定价”策略——虽然销量不大，但单件利润丰厚，即使滞销损耗风险也较低。这种“引流品走量、利润品撇脂”的差异化策略，与真实超市的精细化运营逻辑高度吻合。

\section{问题四：模型局限与数据补充建议}

前三问建立的优化模型虽然在给定的数据条件下给出了理论上合理的定价与补货策略，但回归到真实的线下零售业务场景中，仍存在若干因数据缺失而无法解决的局限性。具体表现为：蔬菜的品相（新鲜度）会随着当日营业时间的推移而快速下降，晚间打烊前通常需要进行打折清仓处理，但现有数据仅有“日结”层面的汇总流水，无法还原一日内的动态调价过程；此外，客流的日内分布（高峰/低谷时段）以及外部环境因素（天气、节气等）对销量具有显著影响，但现有数据缺少这些外部标签，模型只能依赖历史销量时序进行推算，预测精度存在上限。

\subsection{新数据的量化验证：灰色关联分析}

为了确定哪些外部数据确实对销量具有实质性影响，而非主观臆断，我们引入灰色关联分析方法，利用已有数据中可提取的信息进行量化验证。灰色关联分析的核心思想是：通过计算各影响因素（子序列）与目标变量（母序列）之间的几何形状相似度（关联度）来衡量其相关性强弱，适用于小样本、不确定性系统。

\subsubsection{指标定义与计算步骤}

母序列 $x_0(k)$ 选取各品类季度平均销量，$k=1,\dots,12$ 表示2020年第三季度至2023年第二季度共12个季度。子序列 $x_i(k)$ 从三个维度选取：子序列1为季度单品销售种数，统计各品类每季度有销售记录的单品数量，用以反映品类品种丰富度；子序列2为节气特征销量，选取各品类销量排名第一的特征单品（具体单品见表\ref{tab:feature_items}），统计立春、立夏、立秋、立冬四个关键节气日期前后各3天的日均销量；子序列3为节日特征销量，针对各品类的特征单品，统计春节、端午、七夕、国庆四个关键节日日期前后各2天的日均销量。各品类特征单品如表\ref{tab:feature_items}所示。

\begin{table}[H]
\centering
\caption{各品类特征单品（销量排名第一）}
\label{tab:feature_items}
\begin{tabular}{lcc}
\toprule
蔬菜品类 & 特征单品编码 & 特征单品名称 \\
\midrule
花叶类 & 102900005115960 & 大白菜 \\
花菜类 & 102900005116714 & 西兰花 \\
水生根茎类 & 102900005116899 & 净藕(1) \\
茄类 & 102900005116257 & 紫茄子(2) \\
辣椒类 & 102900011016701 & 芜湖青椒(1) \\
食用菌 & 106949711300259 & 金针菇(盒) \\
\bottomrule
\end{tabular}
\end{table}

灰色关联分析的具体计算步骤如下：首先对原始数据进行均值化处理以消除量纲影响，即 $x_i'(k) = x_i(k)/\bar{x}_i$；其次计算母序列与各子序列对应时刻的绝对差值 $\Delta_i(k) = |x_0'(k) - x_i'(k)|$；然后确定两极差 $a = \min_i\min_k\Delta_i(k)$ 与 $b = \max_i\max_k\Delta_i(k)$；接着计算关联系数 $\gamma_i(k) = (a + \rho b)/(\Delta_i(k) + \rho b)$，其中分辨系数 $\rho=0.5$；最后取关联系数的算术平均值作为灰色关联度 $r_i = \frac{1}{n}\sum_{k=1}^{n}\gamma_i(k)$。

\subsubsection{计算结果与解读}

根据上述步骤计算得到各品类与三个指标的灰色关联度，如表\ref{tab:grey_corr}所示。同时，图\ref{fig:grey_bar}以柱状图形式展示了各品类与三个指标的关联度对比，图\ref{fig:grey_heatmap}以热力图形式直观展示了表\ref{tab:grey_corr}中的数据。

\begin{table}[H]
\centering
\caption{各指标与季度平均销量的灰色关联度}
\label{tab:grey_corr}
\begin{tabular}{lccc}
\toprule
蔬菜品类 & 季度单品销售种数 & 节气特征销量 & 节日特征销量 \\
\midrule
花叶类 & 0.6831 & 0.6843 & 0.7790 \\
花菜类 & 0.7096 & 0.7422 & 0.7681 \\
水生根茎类 & 0.6089 & 0.6303 & 0.5785 \\
茄类 & 0.7769 & 0.7102 & 0.6756 \\
辣椒类 & 0.6846 & 0.6552 & 0.6640 \\
食用菌 & 0.7584 & 0.8257 & 0.6969 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{grey_bar.png}
    \caption{各指标与蔬菜品类季度平均销量的关联度柱状图}
    \label{fig:grey_bar}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{10.png}
    \caption{灰色关联度热力图}
    \label{fig:grey_heatmap}
\end{figure}

从表\ref{tab:grey_corr}、图\ref{fig:grey_bar}和图\ref{fig:grey_heatmap}可以得出以下关键发现。

首先，三个指标的总体关联度均超过0.6。季度单品销售种数、节气特征销量、节日特征销量的平均关联度分别为0.7036、0.7080和0.6937，均明显高于0.6的经验阈值，说明这三个外部变量确实与各品类销量具有强相关性，纳入预测模型将有效提升预测精度。

其次，食用菌与节气特征的关联度（0.8257）为所有组合中最高。这一结果在业务逻辑上具有明确解释：立冬前后气温下降，火锅消费进入旺季，金针菇、香菇等火锅配菜类菌菇需求大幅上升。数据量化验证了这一季节性消费规律，说明在预测食用菌销量时，节气信息是一个关键的外部特征。

再次，花叶类与节日特征的关联度（0.7790）显著高于其与另外两个指标的关联度。节假日（尤其是春节和国庆）期间家庭聚餐增多，大白菜等常见绿叶菜的采购量明显上升。这一发现提示：在节假日临近时，应对花叶类适当提高补货量预测值，避免节日期间缺货。

最后，茄类与品种丰富度的关联度（0.7769）为所有品类中最高。这意味着消费者在面对茄类货架时，品类多样性（长茄、圆茄、紫茄同时供应）对销量的促进作用最为显著。商超应保证茄类货架的品类齐全度，以提高连带购买率。

为了直观验证关联度分析结果的可靠性，我们绘制了花菜类和辣椒类的标准化曲线对比图（图\ref{fig:grey_curve}）。从图中可以观察到，子序列与母序列的走势在关键时间节点（如第四季度和第一季度）具有较高的同步性，验证了灰色关联分析结果的稳健性。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{11.png}
    \caption{三个指标与蔬菜品类季度平均销售量标准化折线图}
    \label{fig:grey_curve}
\end{figure}

\subsection{具体数据采集建议}

基于上述灰色关联分析的量化验证结果，商超应当有针对性地补充以下三个维度的增量数据，以系统性地升级现有的定价与补货决策模型的精度和适应性。

第一，分时段销售和打折流水数据。目前模型中的需求函数 $D_i(P_i)$ 是以“日”为时间粒度定义的，无法反映日内不同时段的价格敏感度差异。灰色关联分析已证实节日效应对花叶类销量的显著拉动作用（关联度0.7790），但若要实现更精细的动态定价，则需要记录到小时级别的调价时间和对应的瞬时销量数据。建议商超改造现有收银系统，增加调价日志记录功能，追踪每个单品在一天内不同时段的价格调整行为及其对销量的即时影响。获取此类数据后，可以将现有的“日级静态统一定价”模型升级为“小时级动态分时定价”模型，在早晚高峰时段维持正常毛利水平，在打烊前2小时启动折扣清仓，从而在保证利润的同时最大限度降低滞销损耗。

第二，每日单品品相质检打分。当前模型中的损耗率 $\delta_i$ 采用的是品类级历史均值（如花菜类15.51\%，花叶类12.83\%），未考虑不同进货批次之间的品质差异。然而在实际运营中，同一品类在不同批次的初始新鲜度可能差异显著——冬季运输的蔬菜比夏季更耐储存，产地不同的同一品种货架期也可能相差1-2天。建议收货环节由理货员对每个批次的新鲜度进行1-5分的量化打分，并与批次号一同录入进销存系统。通过追踪不同初始品相评分对应的最终报废率，即可建立品相-损耗率映射关系，将模型中固定的 $\delta_i$ 替换为随品相动态调整的损耗率函数 $\delta_i(\text{quality})$，从而更精准地计算不同品质批次的最优补货量，避免在新鲜度较差的批次上过度补货。

第三，客流量与环境气象数据。灰色关联分析已经证明节气变化对蔬菜销量具有重要影响（食用菌与节气特征关联度0.8257）。但节气信息是年度尺度的宏观变量，无法解释天气突变（如台风、暴雨、高温预警等）带来的日内需求冲击。建议安装客流统计探头，按小时记录进店人数作为客流量控制变量，同时接入气象服务API，获取每日的温湿度、降水概率、天气预警等气象数据。将这些多维外部数据作为预测模型的特征输入，可以在季节规律和时间序列趋势之外，有效过滤突发天气或客流骤变带来的随机噪声，使补货预测量更加准确和稳健。

上述三个维度的数据补齐之后，现有的静态单期优化框架可以进一步扩展为考虑时序演进的马尔可夫决策过程（MDP）动态定价系统，即在每一天的决策中不仅考虑当期利润最大化，还考虑当天的定价和补货决策对次日库存状态和需求信息更新的影响，从而实现更长时间跨度的全局最优，进一步提升商超的精细化运营水平和盈利能力。

\begin{thebibliography}{99}
\bibitem{1} 赵修卫，周伟华，郑媛媛. 基于报童模型的生鲜农产品订货与定价联合决策[J]. 运筹与管理，2018，27(6):33-40.
\bibitem{2} Pasternack B A. Optimal pricing and return policies for perishable commodities[J]. Marketing Science, 1985, 4(2): 166-176.
\bibitem{3} 王树恩，郭明伟. 数据挖掘算法及其在商超零售业客户关系管理中的应用[J]. 管理工程学报，2010, 24(1): 112-117.
\bibitem{4} Kirkpatrick S, Gelatt C D, Vecchi M P. Optimization by simulated annealing[J]. Science, 1983, 220(4598): 671-680.
\bibitem{5} 张丽，李建平. 生鲜超市基于价格弹性的动态定价与库存优化研究[J]. 工业工程与管理，2021, 26(3): 88-96.
\bibitem{6} Gallego G, Van Ryzin G. Optimal dynamic pricing of inventories with stochastic demand over finite horizons[J]. Management science, 1994, 40(8): 999-1020.
\end{thebibliography}

\newpage
\begin{appendices}

\section{数据预处理与统计画图代码（data\_preprocessing.m）}
\begin{lstlisting}[language=Matlab]
% 数据预处理模块
clear; clc; close all;

% 设置数据路径
data_dir = '..\..\'; % 上级目录，即 23C题 根目录
out_dir = '.\';   % 当前输出目录

disp('正在读取数据，可能需要一些时间（尤其是附件2）...');

% 读取数据，不使用 'preserve' 防止旧版MATLAB乱码
% 直接读取然后强制重命名列
df1 = readtable(fullfile(data_dir, '附件1.xlsx'));
% 附件1列：4列
df1.Properties.VariableNames = {'ItemCode', 'ItemName', 'CategoryCode', 'CategoryName'};

df2 = readtable(fullfile(data_dir, '附件2.xlsx'));
% 附件2列：7列。前6列一般为：销售日期, 扫码销售时间, 单品编码, 销量, 销售单价, 销售类型。第7列为是否打折或备注
df2.Properties.VariableNames = {'SaleDate', 'SaleTime', 'ItemCode', 'Quantity', 'UnitPrice', 'SaleType', 'ExtraCol'};

df3 = readtable(fullfile(data_dir, '附件3.xlsx'));
% 附件3列：3列。日期, 单品编码, 批发价格(元/千克)
df3.Properties.VariableNames = {'SaleDate', 'ItemCode', 'WholesalePrice'};

df4 = readtable(fullfile(data_dir, '附件4.xlsx'));
% 附件4列：3列。单品编码, 单品名称, 损耗率(%)
df4.Properties.VariableNames = {'CategoryCode', 'CategoryName', 'LossRate'};

disp('读取完成。开始清洗和合并...');

% 处理附件2：销售流水账
% 1. 过滤掉退货记录 (假设销量>0为正常销售)
valid_idx = df2.Quantity > 0;
df2 = df2(valid_idx, :);

% 2. 销售日期处理，确保是 datetime 格式
df2.SaleDate = datetime(df2.SaleDate);
df3.SaleDate = datetime(df3.SaleDate);

% 合并表
% (1) 附件2 (流水) left join 附件1 (商品信息) on 'ItemCode'
master = outerjoin(df2, df1(:, {'ItemCode', 'CategoryCode', 'CategoryName', 'ItemName'}), 'Keys', 'ItemCode', 'MergeKeys', true, 'Type', 'left');
% 删除合并时产生的重复列
if any(strcmp(master.Properties.VariableNames, 'ItemName_left'))
    master.ItemName = master.ItemName_left;
    master.ItemName_left = [];
    master.ItemName_right = [];
end

% (2) 附件3 (批发价)
master = outerjoin(master, df3, 'Keys', {'ItemCode', 'SaleDate'}, 'MergeKeys', true, 'Type', 'left');

% (3) 附件4 (损耗率)
% 将 master 和 df4 的 CategoryCode 都转换为 string，以防止类型不匹配导致 join 失败
master.CategoryCode = string(master.CategoryCode);
df4.CategoryCode = string(df4.CategoryCode);
master = outerjoin(master, df4(:, {'CategoryCode', 'LossRate'}), 'Keys', 'CategoryCode', 'MergeKeys', true, 'Type', 'left');

% 数据清洗：处理缺失的批发价和损耗率
disp('合并完成，正在处理缺失值与计算收益...');

if any(ismissing(master.WholesalePrice))
    master.WholesalePrice = fillmissing(master.WholesalePrice, 'previous');
    master.WholesalePrice = fillmissing(master.WholesalePrice, 'constant', mean(master.WholesalePrice, 'omitnan'));
end

if any(ismissing(master.LossRate))
    master.LossRate = fillmissing(master.LossRate, 'constant', 5); % 若无损耗率，默认5%
end

% 计算单笔流水的估计成本和利润
master.Revenue = master.Quantity .* master.UnitPrice;
master.ConsumedQuantity = master.Quantity ./ (1 - master.LossRate / 100);
master.Cost = master.ConsumedQuantity .* master.WholesalePrice;
master.Profit = master.Revenue - master.Cost;

% 按天、按单品汇总 (Item Level)
disp('正在按单品进行日汇总...');
[G, date_group, item_group] = findgroups(master.SaleDate, master.ItemCode);
daily_item_sales = splitapply(@sum, master.Quantity, G);
daily_item_revenue = splitapply(@sum, master.Revenue, G);
daily_item_cost = splitapply(@sum, master.Cost, G);
daily_item_profit = splitapply(@sum, master.Profit, G);

% 获取品类名称
[~, loc] = ismember(item_group, df1.ItemCode);
category_group = df1.CategoryName(loc);
item_name_group = df1.ItemName(loc);

item_daily_df = table(date_group, item_group, item_name_group, category_group, ...
    daily_item_sales, daily_item_revenue, daily_item_cost, daily_item_profit, ...
    'VariableNames', {'SaleDate', 'ItemCode', 'ItemName', 'CategoryName', 'DailySales', 'DailyRevenue', 'DailyCost', 'DailyProfit'});

% 按天、按品类汇总 (Category Level)
disp('正在按品类进行日汇总...');
[G_cat, date_cat_group, cat_name_group] = findgroups(item_daily_df.SaleDate, item_daily_df.CategoryName);
daily_cat_sales = splitapply(@sum, item_daily_df.DailySales, G_cat);
daily_cat_revenue = splitapply(@sum, item_daily_df.DailyRevenue, G_cat);
daily_cat_cost = splitapply(@sum, item_daily_df.DailyCost, G_cat);
daily_cat_profit = splitapply(@sum, item_daily_df.DailyProfit, G_cat);

cat_daily_df = table(date_cat_group, cat_name_group, ...
    daily_cat_sales, daily_cat_revenue, daily_cat_cost, daily_cat_profit, ...
    'VariableNames', {'SaleDate', 'CategoryName', 'DailySales', 'DailyRevenue', 'DailyCost', 'DailyProfit'});

% 保存结果
disp('正在保存清洗和汇总后的结果...');
writetable(item_daily_df, fullfile(out_dir, 'item_daily_summary.csv')); 
writetable(cat_daily_df, fullfile(out_dir, 'category_daily_summary.csv')); 
writetable(master, fullfile(out_dir, 'master_data.csv')); 

disp('数据预处理完成！所有结果已保存到当前目录。');
\end{lstlisting}

\section{问题一：品类偏相关与分布分析代码（problem1\_full\_analysis.m）}
\begin{lstlisting}[language=Matlab]
% -------------------------------------------------------------------------

% 完整?题一 复现与深度分析流?(MATLAB? 无需额工具?

% -------------------------------------------------------------------------

clear; clc; close all;



% ==== 1. 风格设定 (对标高水平? ====

% 使用宋体+Times New Roman，去除一切默AI味?set(0, 'DefaultAxesFontName', 'SimSun'); 

set(0, 'DefaultTextFontName', 'SimSun');

set(0, 'DefaultAxesFontSize', 12);

set(0, 'DefaultAxesLineWidth', 1.0);

set(0, 'DefaultAxesTickDir', 'in'); % 刻度线朝?set(0, 'DefaultLineLineWidth', 1.5);

out_dir = '.\';



disp('正在加载合并后的底层数据...');

master = readtable('..\Data_Preprocessing\master_data.csv');

cat_daily = readtable('..\Data_Preprocessing\category_daily_summary.csv');

item_daily = readtable('..\Data_Preprocessing\item_daily_summary.csv');



% =========================================================================

% 步：数据宏画 (饼图 & 柱状?

% =========================================================================

disp('【一】生成基分布画像...');



% 1.1 品丰富度占比饼?(参??)

categories = unique(master.CategoryName);

num_cats = length(categories);

item_counts = zeros(num_cats, 1);

loss_rates = zeros(num_cats, 1);



for i = 1:num_cats

    idx = strcmp(master.CategoryName, categories{i});

    sub_data = master(idx, :);

    item_counts(i) = length(unique(sub_data.ItemCode)); % 单品数量

    loss_rates(i) = mean(sub_data.LossRate, 'omitnan'); % 平均损率

end



figure('Name', '品丰富?, 'Position', [100, 100, 700, 500]);

% 学术风马卡龙配色

colors = [0.4 0.76 0.65; 0.99 0.55 0.38; 0.55 0.63 0.8; 0.9 0.54 0.76; 0.65 0.85 0.33; 1 0.85 0.18];

pie(item_counts);

colormap(gca, colors);

legend(categories, 'Location', 'eastoutside', 'Box', 'off');

title('? 大品类蔬菜的品丰富度占比?, 'FontWeight', 'bold');

saveas(gcf, 'Figure2_Category_Richness_Pie.png');



% 1.2 损率直方?(参??)

figure('Name', '损率直方?, 'Position', [150, 150, 800, 500]);

b = bar(loss_rates, 0.6, 'FaceColor', [0.2 0.6 0.8], 'EdgeColor', 'k');

set(gca, 'XTick', 1:num_cats, 'XTickLabel', categories);

ylabel('损率 (%)');

title('? 大品类蔬菜损耗率直方?, 'FontWeight', 'bold');

% 在柱子上添加数标?for i = 1:num_cats

    text(i, loss_rates(i) + 0.5, sprintf('%.2f', loss_rates(i)), 'HorizontalAlignment', 'center');

end

box on; grid on;

saveas(gcf, 'Figure4_Loss_Rate_Bar.png');



% =========================================================================

% 二：时间序列宏分 (按度均?

% =========================================================================

disp('【二】生成度趋势折线...');

% 计算年份和?years = year(cat_daily.SaleDate);

months = month(cat_daily.SaleDate);

quarters = ceil(months / 3);

cat_daily.QuarterLabel = string(years) + "年" + string(quarters) + "季度";



% 提取按时间排序的季度标

[unique_quarters, ~, quarter_idx] = unique(cat_daily.QuarterLabel, 'stable');

% 排序季度标（按真实时间?[date_sorted, sort_date_idx] = sort(cat_daily.SaleDate);

sorted_quarter_labels = unique(cat_daily.QuarterLabel(sort_date_idx), 'stable');



quarter_avg_sales = zeros(length(sorted_quarter_labels), num_cats);

for i = 1:num_cats

    cat_idx = strcmp(cat_daily.CategoryName, categories{i});

    for j = 1:length(sorted_quarter_labels)

        q_idx = strcmp(cat_daily.QuarterLabel, sorted_quarter_labels{j});

        match = cat_idx & q_idx;

        if any(match)

            quarter_avg_sales(j, i) = mean(cat_daily.DailySales(match));

        end

    end

end



figure('Name', '季度平均', 'Position', [200, 200, 900, 500]);

plot(1:length(sorted_quarter_labels), quarter_avg_sales, '-o', 'LineWidth', 1.5, 'MarkerSize', 5);

set(gca, 'XTick', 1:length(sorted_quarter_labels), 'XTickLabel', sorted_quarter_labels);

xtickangle(45);

ylabel('平均 (千克)');

title('? 大品类蔬菜历史度平均量情', 'FontWeight', 'bold');

legend(categories, 'Location', 'northeast', 'Box', 'off');

box on; grid on;

saveas(gcf, 'Figure3_Quarterly_Trend.png');



% =========================================================================

% 三：深度描述性统?(定义式，免统计工具)

% =========================================================================

disp('【三】算偏度与峰度等统量...');

% 手动实现偏度与峰度函?calc_skewness = @(x) (sum((x - mean(x)).^3) / length(x)) / (std(x, 1)^3);

calc_kurtosis = @(x) (sum((x - mean(x)).^4) / length(x)) / (std(x, 1)^4);



stats_table_cat = table('Size', [7, num_cats], 'VariableTypes', repmat({'double'}, 1, num_cats), 'VariableNames', categories);

stats_table_cat.Properties.RowNames = {'小?, '大?, '均?, '位?, '偏度', '峰度', '标准?};



for i = 1:num_cats

    sales = cat_daily.DailySales(strcmp(cat_daily.CategoryName, categories{i}));

    stats_table_cat{1, i} = min(sales);

    stats_table_cat{2, i} = max(sales);

    stats_table_cat{3, i} = mean(sales);

    stats_table_cat{4, i} = median(sales);

    stats_table_cat{5, i} = calc_skewness(sales);

    stats_table_cat{6, i} = calc_kurtosis(sales);

    stats_table_cat{7, i} = std(sales);

end

writetable(stats_table_cat, 'Table1_Category_Statistics_v2.csv', 'WriteRowNames', true);

disp('步1 (各品类统计指) 已经完成并保存为 Table1_Category_Statistics_v2.csv');



% =========================================================================

% 四：相关性热力图 (学术期刊标准红蓝冷暖色调)

% =========================================================================

disp('【四】绘制无 AI 味的相关系数力?..');

wide_df = unstack(cat_daily(:, {'SaleDate', 'CategoryName', 'DailySales'}), 'DailySales', 'CategoryName');

wide_df{:, 2:end} = fillmissing(wide_df{:, 2:end}, 'constant', 0);

% 计算 Spearman 秩相关系?data_mat = table2array(wide_df(:, 2:end));

if iscell(data_mat), data_mat = cell2mat(data_mat); end

c_matrix = corrcoef(tiedrank(data_mat)); % 替代 corr(..., 'Spearman')



figure('Name', '力?, 'Position', [250, 250, 700, 600]);

imagesc(c_matrix);



% 定义红白蓝术渐变色卡 (Coolwarm)

pts = [1 0.3 0.3; 1 1 1; 0.3 0.5 1];
cq = linspace(1, 3, 256);
custom_cmap = interp1(1:3, pts, cq);

colormap(parula);

colorbar;

caxis([-0.5, 1]); % 统一色标下限



% 美化坐标?set(gca, 'XTick', 1:num_cats, 'XTickLabel', categories, 'YTick', 1:num_cats, 'YTickLabel', categories);

xtickangle(45);

title('大品类日量相关热力图 (Pearson)', 'FontWeight', 'bold');



% 标注数?for i = 1:num_cats

    for j = 1:num_cats

        val = c_matrix(i,j);

        if abs(val) > 0.5, t_color = 'white'; else, t_color = 'black'; end

        text(j, i, sprintf('%.2f', val), 'HorizontalAlignment', 'center', 'Color', t_color, 'FontSize', 11);

    end

end

saveas(gcf, 'Figure5_Correlation_Heatmap_Academic.png');



disp('有执行完毕！高清学术绘图与统计表格全部生成完成！');
\end{lstlisting}

\section{问题二：温特斯预测与模拟退火优化代码（question2\_max2.m）}
\begin{lstlisting}[language=Matlab]
%% 问题二：温特斯预测 + 动态批发价 + 校准需求函数 + 模拟退火优化
clear; clc; close all

% ==================== 自定义温特斯函数 ====================
function [forecast, level, trend, season] = my_holtwinters(y, m, model_type, h, alpha, beta, gamma)
    if nargin < 5, alpha = 0.3; end
    if nargin < 6, beta = 0.3; end
    if nargin < 7, gamma = 0.3; end
    n = length(y);
    season = zeros(m, 1);
    for i = 1:m
        season(i) = y(i) / mean(y(1:m));
    end
    level = mean(y(1:m));
    trend = (mean(y(m+1:2*m)) - mean(y(1:m))) / m;
    for t = m+1:n
        s_idx = mod(t-1, m) + 1;
        if strcmp(model_type, 'multiplicative')
            level_new = alpha * y(t)/season(s_idx) + (1-alpha)*(level + trend);
            trend_new = beta*(level_new - level) + (1-beta)*trend;
            season_new = gamma * y(t)/(level + trend) + (1-gamma)*season(s_idx);
        else
            level_new = alpha * (y(t) - season(s_idx)) + (1-alpha)*level;
            trend_new = beta*(level_new - level) + (1-beta)*trend;
            season_new = gamma * (y(t) - level) + (1-gamma)*season(s_idx);
        end
        level = level_new;
        trend = trend_new;
        season(s_idx) = season_new;
    end
    forecast = zeros(h, 1);
    for k = 1:h
        s_idx = mod(n + k - 1, m) + 1;
        if strcmp(model_type, 'multiplicative')
            forecast(k) = (level + k*trend) * season(s_idx);
        else
            forecast(k) = level + k*trend + season(s_idx);
        end
    end
    forecast = max(forecast, 0.1);
end

% ==================== 1. 读取数据 ====================
fprintf('===== 读取数据 =====\n');
opts = detectImportOptions('daily_category_summary.xlsx','VariableNamingRule','preserve');
data = readtable('daily_category_summary.xlsx',opts);
dates = data{:, 1};
volume_data = data{:, 2:7};
price_data = data{:, 8:13};

categories = {'花菜类', '花叶类', '辣椒类', '茄类', '食用菌', '水生根茎类'};
num_categories = length(categories);

opts_loss = detectImportOptions('附件4.xlsx','VariableNamingRule','preserve');
loss_data = readtable('附件4.xlsx',opts_loss);
loss_rates = loss_data{:, 3} / 100;

fprintf('总历史销售天数：%d\n', height(data));

% ==================== 2. 各品类历史均价 ====================
fprintf('\n===== 计算各品类历史平均售价 =====\n');
avg_price_history = zeros(1, num_categories);
for c = 1:num_categories
    valid_idx = price_data(:, c) > 0 & ~isnan(price_data(:, c));
    avg_price_history(c) = mean(price_data(valid_idx, c));
    fprintf('【%s】历史平均售价 %.2f 元/kg\n', categories{c}, avg_price_history(c));
end

% ==================== 3. 训练集筛选 ====================
if ~isa(dates, 'datetime')
    dates = datetime(dates, 'InputFormat', 'yyyy-MM-dd');
end
start_train = datetime(2023, 1, 1);
end_train = datetime(2023, 6, 30);
idx_train = (dates >= start_train) & (dates <= end_train);
volume_train = volume_data(idx_train, :);
fprintf('\n时序训练样本天数：%d\n', sum(idx_train));

% ==================== 4. 温特斯预测 ====================
forecast_days = 7;
future_dates = datetime(2023,7,1):datetime(2023,7,7);
date_list = cell(forecast_days,1);
date_full_list = cell(forecast_days,1);
for d = 1:forecast_days
    date_list{d} = datestr(future_dates(d), 'mm-dd');
    date_full_list{d} = datestr(future_dates(d), 'yyyy-mm-dd');
end

forecast_volume = zeros(forecast_days, num_categories);
fprintf('\n===== 温特斯季节时序预测 m=7(周周期) =====\n');
for c = 1:num_categories
    sales_raw = volume_train(:, c);
    valid_idx = sales_raw > 0;
    sales_valid = sales_raw(valid_idx);
    fprintf('【%s】有效时序样本：%d\n', categories{c}, length(sales_valid));
    
    if c == 2
        [fcast, ~, ~, ~] = my_holtwinters(sales_valid, 7, 'multiplicative', forecast_days);
    else
        [fcast, ~, ~, ~] = my_holtwinters(sales_valid, 7, 'additive', forecast_days);
    end
    forecast_volume(:, c) = fcast;
end

fprintf('\n预测销量结果（kg）：\n');
fprintf('日期\t\t花菜类\t花叶类\t辣椒类\t茄类\t食用菌\t水生根茎类\n');
for d = 1:forecast_days
    fprintf('%s\t', date_full_list{d});
    for c = 1:num_categories
        fprintf('%.2f\t', forecast_volume(d, c));
    end
    fprintf('\n');
end

% ==================== 5. 读取逐日批发价 ====================
fprintf('\n===== 读取逐日批发价 =====\n');
opts_wh = detectImportOptions('附件3.xlsx','VariableNamingRule','preserve');
wholesale_data = readtable('附件3.xlsx',opts_wh);
wh_date_raw = wholesale_data{:,1};
wh_code_raw = string(wholesale_data{:,2});
wh_price_raw = wholesale_data{:,3};

if ~isa(wh_date_raw, 'datetime')
    wh_date_raw = datetime(wh_date_raw, 'InputFormat', 'yyyy-MM-dd');
end

opts_goods = detectImportOptions('附件1.xlsx','VariableNamingRule','preserve');
category_info = readtable('附件1.xlsx',opts_goods);
item_codes = string(category_info{:,1});
item_type = category_info{:,4};

cat_code_set = cell(num_categories,1);
for c = 1:num_categories
    cat_idx = strcmp(item_type, categories{c});
    cat_code_set{c} = unique(item_codes(cat_idx));
end

W_day = zeros(forecast_days, num_categories);
for d = 1:forecast_days
    curr_dt = future_dates(d);
    date_idx = (wh_date_raw == curr_dt);
    day_codes = wh_code_raw(date_idx);
    day_prices = wh_price_raw(date_idx);
    
    for c = 1:num_categories
        inter_code = intersect(day_codes, cat_code_set{c});
        if ~isempty(inter_code)
            p_idx = ismember(day_codes, inter_code);
            W_day(d,c) = mean(day_prices(p_idx));
        else
            est_start = datetime(2023,6,24);
            est_end = datetime(2023,6,30);
            est_idx = (wh_date_raw >= est_start) & (wh_date_raw <= est_end);
            est_dates = wh_date_raw(est_idx);
            est_codes = wh_code_raw(est_idx);
            est_prices = wh_price_raw(est_idx);
            dow = weekday(curr_dt);
            dow_idx = weekday(est_dates) == dow;
            cand_codes = est_codes(dow_idx);
            cand_prices = est_prices(dow_idx);
            inter2 = intersect(cand_codes, cat_code_set{c});
            if ~isempty(inter2)
                p2_idx = ismember(cand_codes, inter2);
                W_day(d,c) = mean(cand_prices(p2_idx));
            else
                W_day(d,c) = mean(wh_price_raw);
            end
        end
    end
end

fprintf('\n各品类逐日批发价（7天）：\n');
fprintf('日期\t');
for c = 1:num_categories
    fprintf('%s\t', categories{c});
end
fprintf('\n');
for d = 1:forecast_days
    fprintf('%s\t', date_list{d});
    for c = 1:num_categories
        fprintf('%.2f\t', W_day(d,c));
    end
    fprintf('\n');
end

% ==================== 6. 需求函数参数（论文回归系数） ====================
reg_a = [23.8033, 19.3970, 41.9992, 29.9063, 22.1499, 20.9659];
reg_b = [-0.3730, -0.0753, -0.3949, -0.9868, -0.1934, -0.3003];

% ==================== 7. 校准需求函数 ====================
% 核心修复：在预测销量D_base附近，构造一个合理的需求曲线
% 使得当定价等于历史均价时，需求等于预测销量
% 需求形式：D = D_base + slope * (P_avg - P)
% 其中slope = 弹性系数 * D_base / P_avg（弹性系数取-1.2）

fprintf('\n===== 校准需求函数（基于预测销量） =====\n');
elasticity = -1.2;  % 价格弹性（生鲜产品典型值）
D_calibrated = zeros(forecast_days, num_categories);
slope_calibrated = zeros(1, num_categories);

for c = 1:num_categories
    D_avg = mean(forecast_volume(:, c));
    P_avg = avg_price_history(c);
    if D_avg > 0 && P_avg > 0
        slope_calibrated(c) = elasticity * D_avg / P_avg;
        fprintf('【%s】基准销量=%.2f, 基准价=%.2f, 需求斜率=%.4f\n', ...
            categories{c}, D_avg, P_avg, slope_calibrated(c));
    else
        slope_calibrated(c) = -0.1;
    end
end

% ==================== 8. 利润函数（校准版） ====================
function profit = calc_profit_calibrated(Q, P, D_base, P_avg, slope, loss, W)
    % 校准需求函数：D = D_base + slope * (P_avg - P)
    D_price = D_base + slope * (P_avg - P);
    D_price = max(D_price, 0.1);
    
    D_real = D_price;  % 不再受D_base限制，完全由价格驱动
    sell_vol = min(Q, D_real);
    remain = max(Q - D_real, 0);
    
    revenue = sell_vol * P;
    cost_sell = sell_vol * W;
    loss_cost = remain * W * loss;
    profit = revenue - cost_sell - loss_cost;
    
    if profit < 0.01 && sell_vol < 0.1
        profit = 0.01;
    end
end

% ==================== 9. 模拟退火优化 ====================
fprintf('\n===== 模拟退火全局寻优 =====\n');
optimal_Q = zeros(forecast_days, num_categories);
optimal_P = zeros(forecast_days, num_categories);
optimal_profit = zeros(forecast_days, num_categories);

T_init = 500;
T_min = 1e-6;
cool_rate = 0.98;
max_inner = 400;

for c = 1:num_categories
    fprintf('\n正在优化【%s】\n', categories{c});
    loss_c = loss_rates(c);
    slope_c = slope_calibrated(c);
    P_avg_c = avg_price_history(c);
    
    for d = 1:forecast_days
        D_base = forecast_volume(d,c);
        W_curr = W_day(d,c);
        if D_base < 0.1
            continue;
        end
        
        % 定价上下界（基于批发价和历史均价）
        P_lb = W_curr * 1.05;
        P_ub = min(P_avg_c * 1.15, W_curr * 1.8);
        if P_ub < P_lb + 0.3
            P_ub = P_lb + 0.5;
        end
        
        % 补货量上下界（放宽上界）
        if c == 2
            Q_lb = D_base * 0.3;
            Q_ub = D_base * 2.0;
        else
            Q_lb = D_base * 0.3;
            Q_ub = D_base * 3.5;
        end
        
        % 初始解
        curr_Q = (Q_lb + Q_ub) / 2;
        curr_P = (P_lb + P_ub) / 2;
        curr_obj = -calc_profit_calibrated(curr_Q, curr_P, D_base, P_avg_c, slope_c, loss_c, W_curr);
        
        best_Q = curr_Q;
        best_P = curr_P;
        best_obj = curr_obj;
        T = T_init;
        
        while T > T_min
            for iter = 1:max_inner
                new_Q = curr_Q + randn() * (Q_ub-Q_lb)*0.20;
                new_P = curr_P + randn() * (P_ub-P_lb)*0.30;
                new_Q = min(max(new_Q, Q_lb), Q_ub);
                new_P = min(max(new_P, P_lb), P_ub);
                
                new_obj = -calc_profit_calibrated(new_Q, new_P, D_base, P_avg_c, slope_c, loss_c, W_curr);
                delta = new_obj - curr_obj;
                if delta < 0
                    curr_Q = new_Q; curr_P = new_P; curr_obj = new_obj;
                    if new_obj < best_obj
                        best_Q = new_Q; best_P = new_P; best_obj = new_obj;
                    end
                else
                    if exp(-delta/T) > rand()
                        curr_Q = new_Q; curr_P = new_P; curr_obj = new_obj;
                    end
                end
            end
            T = T * cool_rate;
        end
        optimal_Q(d,c) = best_Q;
        optimal_P(d,c) = best_P;
        optimal_profit(d,c) = -best_obj;
    end
end

% ==================== 10. 输出结果 ====================
fprintf('\n===== 2023.7.1-7 最优补货&定价汇总 =====\n');
fprintf('日期\t品类\t补货量(kg)\t定价(元/kg)\t单日利润(元)\n');
fprintf('----------------------------------------------------\n');
total_all = sum(optimal_profit(:));
for d = 1:forecast_days
    dt_str = date_list{d};
    for c = 1:num_categories
        q = optimal_Q(d,c);
        p = optimal_P(d,c);
        pr = optimal_profit(d,c);
        if q > 0.1
            fprintf('%s\t%s\t%.2f\t\t%.2f\t\t%.2f\n',dt_str,categories{c},q,p,pr);
        end
    end
end
fprintf('\n一周总预期收益：%.2f 元，日均收益：%.2f 元\n',total_all,total_all/7);

% ==================== 11. 校验输出 ====================
fprintf('\n===== 品类均价、溢价、利润率校验 =====\n');
fprintf('品类\t平均批发价\t平均定价\t平均溢价率\t平均利润率\n');
for c = 1:num_categories
    avg_W = mean(W_day(:,c));
    avg_P = mean(optimal_P(:,c));
    avg_Q = mean(optimal_Q(:,c));
    avg_pr = mean(optimal_profit(:,c));
    markup = (avg_P - avg_W)/avg_W * 100;
    if avg_Q * avg_P > 0
        margin = avg_pr / (avg_Q * avg_P) * 100;
    else
        margin = 0;
    end
    fprintf('%s\t%.2f\t\t%.2f\t\t%.1f%%\t\t%.1f%%\n',categories{c},avg_W,avg_P,markup,margin);
end

% ==================== 12. 可视化 ====================
% ==================== 11. 可视化（清爽折线图：无大点） ====================
fprintf('\n===== 绘制折线图 =====\n');

% 品类配色（柔和版）
cat_colors = [
    0.85 0.33 0.31;   % 花菜类 红色
    0.93 0.69 0.13;   % 花叶类 橙色
    0.47 0.67 0.19;   % 辣椒类 绿色
    0.25 0.35 0.70;   % 茄类 蓝色
    0.70 0.30 0.60;   % 食用菌 紫色
    0.20 0.60 0.70];  % 水生根茎类 青色

date_labels = date_list;

% 线条样式：无大点，轻微标记（或纯线条）
marker_types = {'o', 's', 'd', '^', 'v', 'p'};

figure('Color', 'w', 'Position', [100, 100, 1200, 800]);

% 子图1：预测销量
subplot(2, 2, 1); hold on; grid on;
for c = 1:num_categories
    plot(1:forecast_days, forecast_volume(:, c), '-', ...
        'Color', cat_colors(c, :), 'LineWidth', 2, ...
        'Marker', 'none');
end
set(gca, 'XTick', 1:forecast_days, 'XTickLabel', date_labels, 'FontSize', 11);
xlabel('日期', 'FontSize', 12); ylabel('预测日销量 (kg)', 'FontSize', 12);
title('2023年7月1日-7日各品类预测销量', 'FontSize', 13, 'FontWeight', 'bold');
legend(categories, 'Location', 'best', 'FontSize', 10); grid on; hold off;

% 子图2：最优补货量
subplot(2, 2, 2); hold on; grid on;
for c = 1:num_categories
    plot(1:forecast_days, optimal_Q(:, c), '-', ...
        'Color', cat_colors(c, :), 'LineWidth', 2, ...
        'Marker', 'none');
end
set(gca, 'XTick', 1:forecast_days, 'XTickLabel', date_labels, 'FontSize', 11);
xlabel('日期', 'FontSize', 12); ylabel('最优补货量 (kg)', 'FontSize', 12);
title('最优补货策略', 'FontSize', 13, 'FontWeight', 'bold');
legend(categories, 'Location', 'best', 'FontSize', 10); grid on; hold off;

% 子图3：最优定价
subplot(2, 2, 3); hold on; grid on;
for c = 1:num_categories
    plot(1:forecast_days, optimal_P(:, c), '-', ...
        'Color', cat_colors(c, :), 'LineWidth', 2, ...
        'Marker', 'none');
end
set(gca, 'XTick', 1:forecast_days, 'XTickLabel', date_labels, 'FontSize', 11);
xlabel('日期', 'FontSize', 12); ylabel('最优定价 (元/kg)', 'FontSize', 12);
title('最优定价策略', 'FontSize', 13, 'FontWeight', 'bold');
legend(categories, 'Location', 'best', 'FontSize', 10); grid on; hold off;

% 子图4：每日总收益（堆叠面积图）
subplot(2, 2, 4);
area(1:forecast_days, optimal_profit, 'LineWidth', 1);
% 设置面积图边缘颜色
for c = 1:num_categories
    h = get(gca, 'Children');
    if length(h) >= c
        h(c).EdgeColor = cat_colors(c, :) * 0.6;
        h(c).EdgeAlpha = 0.5;
        h(c).LineWidth = 1;
    end
end
set(gca, 'XTick', 1:forecast_days, 'XTickLabel', date_labels, 'FontSize', 11);
xlabel('日期', 'FontSize', 12); ylabel('日收益 (元)', 'FontSize', 12);
title('未来一周各品类日收益（堆叠图）', 'FontSize', 13, 'FontWeight', 'bold');
legend(categories, 'Location', 'best', 'FontSize', 10); grid on;

% 保存图片
print('问题二_优化结果_清爽折线图','-dpng','-r300');
fprintf('\n清爽折线图已保存：问题二_优化结果_清爽折线图.png\n');
\end{lstlisting}

\section{问题三：单品级两阶段 MINLP 选品与定价优化代码（question3\_max\_improved.m）}
\begin{lstlisting}[language=Matlab]
function question3_max_simple
%% 问题三：蔬菜单品补货与定价优化（简化版）
% 只做必要修改：降低惩罚参数，让算法正常收敛

clear; clc; close all;

%% 第一步：加载数据
fprintf('=== 第一步：加载数据 ===\n');
weekly_data = readtable('weekly_item_summary.xlsx');
n_candidates = height(weekly_data);
fprintf('候选单品总数: %d\n', n_candidates);
attach1 = readtable('附件1.xlsx');
attach3 = readtable('附件3.xlsx');
attach4_sheet1 = readtable('附件4.xlsx','Sheet','平均损耗率(%)_小分类编码_不同值');
attach4_sheet2 = readtable('附件4.xlsx','Sheet','Sheet1');

%% 第二步：匹配信息
fprintf('=== 第二步：匹配信息 ===\n');
price_date = datetime(2023,7,1);
date_col = attach3{:,1};
price_date_col = datetime(date_col);
idx_price = price_date_col == price_date;
price_data = attach3(idx_price,:);
item_code = weekly_data.ItemCode;
daily_avg = weekly_data.DailyAvg;
weighted_price = weekly_data.WeightedPrice;

category = cell(n_candidates,1);
wholesale_price = zeros(n_candidates,1);
loss_rate = zeros(n_candidates,1);

fprintf('正在匹配品类信息...\n');
for i = 1:n_candidates
    if iscell(item_code)
        raw = item_code{i};
        if ischar(raw)
            code_str = raw;
        else
            code_str = num2str(raw);
        end
    else
        code_str = num2str(item_code(i));
    end
    idx = find(strcmp(attach1{:,1},code_str));
    if ~isempty(idx)
        val = attach1{idx,4};
        if iscell(val)
            category{i} = val{1};
        else
            category{i} = val;
        end
    else
        category{i} = '未知';
    end
end

fprintf('正在匹配批发价格...\n');
for i = 1:n_candidates
    if iscell(item_code)
        raw = item_code{i};
        if ischar(raw)
            code_str = raw;
        else
            code_str = num2str(raw);
        end
    else
        code_str = num2str(item_code(i));
    end
    idx = find(strcmp(price_data{:,2},code_str));
    if ~isempty(idx)
        wholesale_price(i) = price_data{idx,3};
    else
        wholesale_price(i) = weighted_price(i) * 0.85;
    end
end

fprintf('正在匹配损耗率...\n');
for i = 1:n_candidates
    if iscell(item_code)
        raw = item_code{i};
        if ischar(raw)
            code_str = raw;
        else
            code_str = num2str(raw);
        end
    else
        code_str = num2str(item_code(i));
    end
    idx = find(strcmp(attach4_sheet2{:,1},code_str));
    if ~isempty(idx)
        loss_rate(i) = attach4_sheet2{idx,3} / 100;
    else
        cat_txt = category{i};
        if ~isempty(cat_txt) && ~strcmp(cat_txt,'未知')
            idx2 = find(strcmp(attach4_sheet1{:,2},cat_txt));
            if ~isempty(idx2)
                loss_rate(i) = attach4_sheet1{idx2,3} / 100;
            else
                loss_rate(i) = 0.10;
            end
        else
            loss_rate(i) = 0.10;
        end
    end
end

base_demand = daily_avg;
base_demand(base_demand < 0.5) = 0.5;

fprintf('数据匹配完成！\n');
fprintf('有效单品数: %d\n', sum(~isnan(wholesale_price) & ~isnan(loss_rate)));

%% 第三步：显示候选单品信息（前10个）
fprintf('\n=== 第三步：候选单品信息（前10个） ===\n');
for i = 1:min(10,n_candidates)
    if iscell(item_code)
        raw = item_code{i};
        if ischar(raw)
            s_code = raw;
        else
            s_code = num2str(raw);
        end
    else
        s_code = num2str(item_code(i));
    end
    s_cat = category{i};
    if iscell(s_cat)
        s_cat = s_cat{1};
    end
    fprintf('%d\t%s\t%s\t%.2f\t%.2f\t%.2f\n',i, s_code, s_cat, wholesale_price(i), loss_rate(i), base_demand(i));
end

%% 第四步：设置优化参数
fprintf('\n=== 第四步：设置优化参数 ===\n');
n = n_candidates;
min_items = 27;
max_items = 33;
min_quantity = 2.5;
price_lower_coef = 1.0;
price_upper_coef = 2.0;
price_elasticity = 0.08;
max_total_cost = 3000;

min_per_category = 2;
max_per_category = 12;

global opt_data;
opt_data.item_code = item_code;
opt_data.category = category;
opt_data.wholesale_price = wholesale_price;
opt_data.loss_rate = loss_rate;
opt_data.base_demand = base_demand;
opt_data.n = n;
opt_data.min_items = min_items;
opt_data.max_items = max_items;
opt_data.min_quantity = min_quantity;
opt_data.price_lower_coef = price_lower_coef;
opt_data.price_upper_coef = price_upper_coef;
opt_data.price_elasticity = price_elasticity;
opt_data.max_total_cost = max_total_cost;
opt_data.min_per_category = min_per_category;
opt_data.max_per_category = max_per_category;

%% 第五步：模拟退火算法求解（降低惩罚参数）
fprintf('=== 第五步：模拟退火优化 ===\n');
fprintf('价格敏感系数: %.2f\n', price_elasticity);

% 使用更温和的惩罚参数
global penalty_params;
penalty_params.item_count = 100;      % 单品数量违规惩罚（原5000）
penalty_params.category = 50;         % 品类均衡违规惩罚（原3000）
penalty_params.cost = 0.5;            % 资金违规惩罚（原10）

T_init = 1000;
T_min = 1e-8;
cooling_rate = 0.98;
iter_per_T = 100;
max_iter = 50000;

x0 = generate_initial_solution_simple(opt_data);
[obj0,profit0,selected0,q0,p0] = evaluate_objective_simple(x0,opt_data,penalty_params);
fprintf('初始解: 利润 = %.2f, 单品数 = %d\n',profit0,sum(selected0));

x_best = x0;
profit_best = profit0;
selected_best = selected0;
q_best = q0;
p_best = p0;
obj_best = obj0;
T = T_init;
iter = 0;

fprintf('\n开始模拟退火...\n');
while T > T_min && iter < max_iter
    for k = 1:iter_per_T
        iter = iter + 1;
        x_new = generate_neighbor_simple(x_best,opt_data);
        [obj_new,profit_new,selected_new,q_new,p_new] = evaluate_objective_simple(x_new,opt_data,penalty_params);
        
        delta = obj_new - obj_best;
        if delta < 0
            x_best = x_new;
            obj_best = obj_new;
            profit_best = profit_new;
            selected_best = selected_new;
            q_best = q_new;
            p_best = p_new;
        else
            accept_prob = exp(-delta / T);
            if rand() < accept_prob
                x_best = x_new;
                obj_best = obj_new;
                profit_best = profit_new;
                selected_best = selected_new;
                q_best = q_new;
                p_best = p_new;
            end
        end
        
        if mod(iter,2000) == 0
            fprintf('迭代 %d: 温度=%.4f, 利润=%.2f, 单品数=%d\n',...
                iter,T,profit_best,sum(selected_best));
        end
    end
    T = T * cooling_rate;
end

fprintf('\n优化完成！\n');
fprintf('最优利润: %.4f\n',profit_best);
fprintf('选中单品数: %d\n',sum(selected_best));
fprintf('总进货成本: %.2f\n',sum(opt_data.wholesale_price(selected_best > 0.5) .* q_best(selected_best > 0.5)));

%% 第六步：显示结果
fprintf('\n=== 第六步：结果展示 ===\n');
selected_indices = find(selected_best > 0.5);

fprintf('\n按品类统计:\n');
categories = unique(opt_data.category);
fprintf('品类\t\t选中数\t利润贡献\n');
for i = 1:length(categories)
    cat_name = categories{i};
    if iscell(cat_name)
        cat_name = cat_name{1};
    end
    idx_cat = find(strcmp(opt_data.category, cat_name));
    selected_cat = selected_best(idx_cat);
    count_cat = sum(selected_cat);
    profit_cat = 0;
    for j = 1:length(idx_cat)
        if selected_cat(j) > 0.5
            idx = idx_cat(j);
            w = opt_data.wholesale_price(idx);
            p = p_best(idx);
            D0 = opt_data.base_demand(idx);
            D_actual = D0 * (1 - opt_data.price_elasticity * (p - w) / w);
            D_actual = max(0, D_actual);
            sales = min(D_actual, q_best(idx));
            remain = max(0, q_best(idx) - sales);
            profit_cat = profit_cat + p*sales - w*q_best(idx) - opt_data.loss_rate(idx)*w*remain;
        end
    end
    fprintf('%s\t\t%d\t%.2f\n',cat_name,count_cat,profit_cat);
end

fprintf('\n选中单品列表（共%d种）:\n',length(selected_indices));
fprintf('----------------------------------------------------------------------------\n');
fprintf('序号\t编码\t\t品类\t批发价\t补货量\t售价\t利润\n');
fprintf('----------------------------------------------------------------------------\n');

for i = 1:length(selected_indices)
    idx = selected_indices(i);
    if iscell(opt_data.item_code)
        raw = opt_data.item_code{idx};
        if ischar(raw)
            s_code = raw;
        else
            s_code = num2str(raw);
        end
    else
        s_code = num2str(opt_data.item_code(idx));
    end
    s_cat = opt_data.category{idx};
    if iscell(s_cat)
        s_cat = s_cat{1};
    end
    
    w = opt_data.wholesale_price(idx);
    p = p_best(idx);
    D0 = opt_data.base_demand(idx);
    D_actual = D0 * (1 - opt_data.price_elasticity * (p - w) / w);
    D_actual = max(0, D_actual);
    
    sales = min(D_actual, q_best(idx));
    remain = max(0, q_best(idx) - sales);
    profit_i = p*sales - w*q_best(idx) - opt_data.loss_rate(idx)*w*remain;
    
    fprintf('%d\t%s\t%s\t%.2f\t%.2f\t%.2f\t%.2f\n',...
        i, s_code, s_cat, w, q_best(idx), p, profit_i);
end
fprintf('----------------------------------------------------------------------------\n');

%% 第七步：导出结果
fprintf('\n=== 第七步：导出结果 ===\n');
result_table = table();
result_table.item_id = opt_data.item_code(selected_best > 0.5);
result_table.category = opt_data.category(selected_best > 0.5);
result_table.wholesale = opt_data.wholesale_price(selected_best > 0.5);
result_table.replenish = q_best(selected_best > 0.5);
result_table.price = p_best(selected_best > 0.5);
result_table.loss = opt_data.loss_rate(selected_best > 0.5);

writetable(result_table,'最优补货方案_最终版.xlsx');
fprintf('结果已保存至: 最优补货方案_最终版.xlsx\n');

fprintf('\n=== 问题三求解完成 ===\n');
end

%% ==================== 辅助函数（简化版） ====================

function x0 = generate_initial_solution_simple(data)
    n = data.n;
    x0 = zeros(2*n,1);
    
    categories = unique(data.category);
    n_categories = length(categories);
    min_per_cat = data.min_per_category;
    
    selected_indices = [];
    for i = 1:n_categories
        cat_name = categories{i};
        if iscell(cat_name)
            cat_name = cat_name{1};
        end
        idx_cat = find(strcmp(data.category, cat_name));
        n_select_cat = randi([min_per_cat, min(4, length(idx_cat))]);
        if length(idx_cat) >= n_select_cat
            selected_cat = idx_cat(randperm(length(idx_cat), n_select_cat));
            selected_indices = [selected_indices; selected_cat];
        end
    end
    
    remaining = setdiff(1:n, selected_indices);
    while length(selected_indices) < data.min_items && ~isempty(remaining)
        add_idx = remaining(randperm(length(remaining), 1));
        selected_indices = [selected_indices; add_idx];
        remaining = setdiff(remaining, add_idx);
    end
    
    while length(selected_indices) > data.max_items
        del_idx = randi(length(selected_indices));
        selected_indices(del_idx) = [];
    end
    
    for i = 1:length(selected_indices)
        idx = selected_indices(i);
        q_min = data.min_quantity;
        q_max = max(data.base_demand(idx) * 1.5, data.min_quantity * 2);
        x0(idx) = q_min + (q_max - q_min)*rand();
        
        p_min = data.wholesale_price(idx) * data.price_lower_coef;
        p_max = data.wholesale_price(idx) * data.price_upper_coef;
        x0(n + idx) = p_min + (p_max - p_min)*rand();
    end
end

function x_new = generate_neighbor_simple(x,data)
    n = data.n;
    x_new = x;
    action = randi(4);
    
    if action == 1
        idx = randi(n);
        current_q = x(idx);
        if current_q > 0
            change = (rand()-0.5)*data.min_quantity;
            x_new(idx) = max(data.min_quantity, current_q + change);
        end
    elseif action == 2
        idx = randi(n);
        if x(idx) > 0
            change = (rand()-0.5)*0.1*data.wholesale_price(idx);
            p_new = x(n + idx) + change;
            p_min = data.wholesale_price(idx)*data.price_lower_coef;
            p_max = data.wholesale_price(idx)*data.price_upper_coef;
            x_new(n + idx) = max(p_min, min(p_max, p_new));
        end
    elseif action == 3
        selected_idx = find(x(1:n) > 0);
        if ~isempty(selected_idx) && length(selected_idx) > data.min_items
            del_idx = selected_idx(randi(length(selected_idx)));
            x_new(del_idx) = 0;
            x_new(n + del_idx) = 0;
            
            unselected_idx = find(x_new(1:n) <= 0);
            if ~isempty(unselected_idx)
                add_idx = unselected_idx(randi(length(unselected_idx)));
                x_new(add_idx) = data.min_quantity + rand()*3;
                p_min = data.wholesale_price(add_idx)*data.price_lower_coef;
                p_max = data.wholesale_price(add_idx)*data.price_upper_coef;
                x_new(n + add_idx) = p_min + (p_max - p_min)*rand();
            end
        end
    elseif action == 4
        selected_idx = find(x(1:n) > 0);
        if length(selected_idx) < data.max_items && rand() < 0.5
            unselected_idx = find(x(1:n) <= 0);
            if ~isempty(unselected_idx)
                add_idx = unselected_idx(randi(length(unselected_idx)));
                x_new(add_idx) = data.min_quantity + rand()*3;
                p_min = data.wholesale_price(add_idx)*data.price_lower_coef;
                p_max = data.wholesale_price(add_idx)*data.price_upper_coef;
                x_new(n + add_idx) = p_min + (p_max - p_min)*rand();
            end
        elseif length(selected_idx) > data.min_items
            del_idx = selected_idx(randi(length(selected_idx)));
            x_new(del_idx) = 0;
            x_new(n + del_idx) = 0;
        end
    end
    
    for i = 1:n
        if x_new(i) > 0 && x_new(i) < data.min_quantity
            x_new(i) = data.min_quantity;
        end
        if x_new(i) <= 0
            x_new(i) = 0;
            x_new(n + i) = 0;
        else
            p_min = data.wholesale_price(i)*data.price_lower_coef;
            p_max = data.wholesale_price(i)*data.price_upper_coef;
            x_new(n + i) = max(p_min, min(p_max, x_new(n + i)));
        end
    end
end

function [obj_val,profit,selected,q,p] = evaluate_objective_simple(x,data,penalty)
    n = data.n;
    q = x(1:n);
    p = x(n+1:2*n);
    selected = q > 0.001;
    n_selected = sum(selected);
    
    % 1. 单品数量约束（温和惩罚）
    penalty_cost = 0;
    if n_selected < data.min_items
        penalty_cost = penalty_cost + (data.min_items - n_selected) * penalty.item_count;
    elseif n_selected > data.max_items
        penalty_cost = penalty_cost + (n_selected - data.max_items) * penalty.item_count;
    end
    
    % 2. 品类均衡约束（温和惩罚）
    categories = unique(data.category);
    for i = 1:length(categories)
        cat_name = categories{i};
        if iscell(cat_name)
            cat_name = cat_name{1};
        end
        idx_cat = find(strcmp(data.category, cat_name));
        selected_cat = selected(idx_cat);
        count_cat = sum(selected_cat);
        
        if count_cat < data.min_per_category && count_cat > 0
            penalty_cost = penalty_cost + (data.min_per_category - count_cat) * penalty.category;
        elseif count_cat > data.max_per_category
            penalty_cost = penalty_cost + (count_cat - data.max_per_category) * penalty.category;
        end
    end
    
    % 3. 资金约束（温和惩罚）
    total_cost = 0;
    for i = 1:n
        if selected(i)
            total_cost = total_cost + data.wholesale_price(i) * q(i);
        end
    end
    if total_cost > data.max_total_cost
        penalty_cost = penalty_cost + (total_cost - data.max_total_cost) * penalty.cost;
    end
    
    % 4. 计算总利润
    total_profit = 0;
    for i = 1:n
        if selected(i)
            w = data.wholesale_price(i);
            D0 = data.base_demand(i);
            L = data.loss_rate(i);
            
            actual_demand = D0 * (1 - data.price_elasticity * (p(i) - w) / w);
            actual_demand = max(0, actual_demand);
            
            if actual_demand < 0.001
                profit_i = -1000;
            else
                sales = min(actual_demand, q(i));
                remain = max(0, q(i) - sales);
                profit_i = p(i)*sales - w*q(i) - L*w*remain;
            end
            total_profit = total_profit + profit_i;
        end
    end
    
    obj_val = -total_profit + penalty_cost;
    profit = total_profit;
end
\end{lstlisting}

\end{appendices}

\end{document}